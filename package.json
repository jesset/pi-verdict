{
  "name": "pi-auto-mode",
  "private": true,
  "version": "0.1.0",
  "description": "Auto Mode extension prototype for pi coding agent (rules + model classifier gating for tool calls)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  }
}
